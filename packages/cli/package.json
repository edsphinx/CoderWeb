{
  "name": "codewrite",
  "version": "1.0.0",
  "description": "",
  "bin": "dist/index.js",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "tsc --watch --preserveWatchOutput",
    "prepublishOnly": "esbuild src/index.ts --platform=node --outfile=dist/index.js --bundle --minify --define:process.env.NODE_ENV=\\\"production\\\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@codewrite/local-client": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.1.3",
    "esbuild": "^0.17.19",
    "typescript": "^5.0.4",
    "@codewrite/local-api": "^0.1.0",
    "commander": "^10.0.1"
  }
}
